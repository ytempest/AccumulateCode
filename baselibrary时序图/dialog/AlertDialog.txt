AlertDialog dialog = new AlertDialog.Builder(MainActivity.this)
			.setContentView(R.layout.detail_comment_dialog)
			.show();
			
执行流程：
Builder(Context):
		AlertController.AlertParams(Context, int):
setContentView(int)&Builder：
show()&Builder:
		create()&AlertDialog:
				AlertDialog(Context,int):
						AlertController(AlertDialog, Window):
				AlertController.AlertParams.apply(AlertController)&void:
							DialogViewHelper(Context, int):
							DialogViewHelper.setContentView(View)&void:
							AlertController.setViewHelper(ViewHelper)&void:
							AlertController.getDialog()&AlertDialog:
							AlertController.setText(int, CharSequence)&void:
									DialogViewHelper.setText(int, CharSequence)&void:
							AlertController.setOnclickListener(int, View.OnClickListener))&void:
									DialogViewHelper.setOnclickListener(int, View.OnClickListener)&void: